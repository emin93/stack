# install

One-command provisioning for a fresh Apple Silicon Mac.

```bash
bash -c "$(curl -fsSL https://install.emin.ch/install.sh)"
```

See [install.emin.ch](https://install.emin.ch) for what gets installed and how it works.
