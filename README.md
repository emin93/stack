# stack

One-command provisioning for a fresh Apple Silicon Mac.

```bash
bash -c "$(curl -fsSL https://stack.emin.ch/install.sh)"
```

See [stack.emin.ch](https://stack.emin.ch) for what gets installed and how it works.
